# Minimum-version pins. These are the versions this project was built and
# tested against; newer patch releases are fine. Exact pins ("==") are
# brittle across platforms and Python versions, which is the last thing you
# want a judge to hit on a fresh clone.

# --- Web ---
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1.4
python-multipart>=0.0.9

# --- Agents / LLM (required: challenge needs a named LLM client) ---
langgraph>=0.2.60
langgraph-checkpoint-sqlite>=2.0.1
tenacity>=9.0

# LLM providers. Swap with LLM_PROVIDER in .env — no code changes needed.
# The openai SDK also serves Groq, Gemini, OpenRouter and Ollama, which all
# expose OpenAI-compatible endpoints.
openai>=1.55
anthropic>=0.40

# --- Persistence ---
sqlalchemy>=2.0.30

# --- Config / auth ---
pydantic>=2.9
pydantic-settings>=2.5
python-jose[cryptography]>=3.3

# --- Dev / seed / tests ---
faker>=30.0
pytest>=8.0
httpx>=0.27

# --- Optional: only if DATABASE_URL points at Postgres instead of SQLite ---
# psycopg[binary]>=3.2
